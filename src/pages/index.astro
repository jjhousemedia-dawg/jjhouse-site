---
import Scroll from '../layouts/Scroll.astro';
import data from '../data/projects.json';
import Wordmark from '../components/Wordmark.astro';

// --- beat 05 selection -------------------------------------------------
// PLACEHOLDER PICK. Chosen to cover range and to include the two projects
// JJ has already named as anecdotes (Music is Better, The Districts).
// Swap these slugs when he confirms the real six.
const FEATURED = [
  'charli-xcx-used-to-know-me',
  'rufus-du-sol-music-is-better',
  'magdalena-bay-chaeri',
  'the-districts-outlaw-love',
  'finneas-cleats',
  'amiri-pacific-flat',
];

const bySlug = Object.fromEntries(data.projects.map((p) => [p.slug, p]));
const featured = FEATURED.map((s) => bySlug[s]).filter(Boolean);

// Little Dot gets its own beat. Figures are JJ's verified numbers.
const LDS = { channels: '15+', views: '5B+', subs: '5.1M' };

const sizes = ['project--l', 'project--r', 'project--xl', 'project--l', 'project--r', 'project--xl'];
---

<Scroll loader={true} title="JJ House — Creative strategist" desc="JJ House. Creative strategist in Los Angeles.">
  <!-- ================= 01 ARRIVAL ================= -->
  <header class="beat arrival">
    <div class="arrival__top">
      <div>
        <p class="arrival__deck">
          I make work for screens, and I&rsquo;m usually in it from the first idea to the last frame.
        </p>
        <!-- Board 2 + noth.in both carry a CTA in the hero. The only CTA on the
             page was 9,700px down, for a hiring-manager audience. -->
        <p class="arrival__cta">
          <a class="btn" href="#comein"><span>Book a call &rarr;</span></a>
        </p>
      </div>
      <button class="arrival__menu" id="menuBtn" aria-expanded="false">Menu ::</button>
    </div>

    <!-- Board 2. Surface minimal, substrate maximal. Drag to erode; it
         refills like foam closing over water. The canvas here is a 2D
         stand-in for the WebGL2 mask-reveal (decay ~0.94, stamp ~8vw,
         noise matched to hair scale). Substrate becomes Blender geo-nodes
         work, in colour, per spec §5. -->
    <!-- The ink layer spans the whole hero, NOT the wordmark box. That is the
         point of the reference: the letterforms are part of one sheet lying
         over the water, and the cursor shoves that sheet around, so torn ink
         has to be able to pile up well outside the letters. -->
    <canvas class="surface__erode" id="erode"></canvas>

    <div class="surface" id="surface">
      <span class="wire-tag">Wire / board 02 &mdash; move across the mark</span>
      <div class="surface__substrate"><span>[ substrate: swap the water loop for the Blender render ]</span></div>
      <div class="wordmark__svg"><Wordmark /></div>
    </div>

    <div class="arrival__foot">
      <span class="meta">Creative strategist &middot; Los Angeles</span>
      <span class="meta">LinkedIn / <a href="https://www.instagram.com/jj.duplex/" style="color:inherit">Instagram</a> / Resume</span>
    </div>
  </header>

  <!-- ================= 02 PREMISE ================= -->
  <section class="beat beat--full">
    <div class="beat__inner inkproof">
      <h1 class="statement" data-rise>The work you deliver is never the work you pitched.</h1>
    </div>
  </section>

  <!-- ================= 03 ASIDE ================= -->
  <section class="beat">
    <div class="beat__inner inkproof" data-rise>
      <p class="label">( the premise )</p>
      <div class="prose">
        <p>
          The money goes, the days go, and you make what you can. Sometimes the piece works out what
          it&rsquo;s actually about, and done well, that&rsquo;s the version everyone later claims to
          have intended. The rest is just loss, and both can feel the same while you&rsquo;re in it.
        </p>
        <p>
          Produced, cut, done the VFX, run the set, you name it. I learned all of it twice over, once
          because I wanted to be good and once because running a company meant I couldn&rsquo;t afford
          not to. What that&rsquo;s good for is telling the two apart while there&rsquo;s still time
          to act.
        </p>
        <p>The work below includes making-of anecdotes. Some fun surprises in there. Enjoy.</p>
      </div>
    </div>
  </section>

  <!-- ================= 04 REEL — the one-way flip ================= -->
  <section class="reel" id="reel">
    <div class="reel__sticky">
      <!-- Board 9. The pink drawing on JJ's board is the environment the
           video sits inside. Grid below stands in for that room until the
           Blender plate exists. Spec §9: biggest single asset, still TBD. -->
      <div class="reel__room" aria-hidden="true"></div>

      <button class="reel__sound" id="soundBtn" aria-pressed="false"><span>Sound</span></button>

      <div class="reel__frame" id="reelFrame">
        <div class="ph" style="width:min(90%,60rem);aspect-ratio:16/9">
          REEL &mdash; beauty plate
          <small>enters full bleed, recedes with perspective as you scroll</small>
          <small>board 4: returns to full size on scroll up</small>
        </div>
        <!-- Board 9 + verified on noth.in: CLEAN.mp4 stacked directly above
             REFLECT.mp4, exactly one video height apart. Separate pass, not
             a CSS trick, once the Blender render exists. -->
        <div class="ph reel__reflect" style="width:min(90%,60rem);aspect-ratio:16/5" aria-hidden="true">
          reflection pass
        </div>
      </div>

      <p class="meta" style="position:absolute;bottom:1.25rem;left:50%;transform:translateX(-50%);opacity:.4;text-align:center;max-width:44rem;padding:0 1rem">
        [ board 4 &mdash; ruled 2026-08-11: the line exists, without any house
        wordplay. JJ&rsquo;s words to come. ]
      </p>
    </div>
  </section>

  <!-- ================= 05 WORK ================= -->
  <section class="beat" id="work">
    <!-- Board 5, verified live on noth.in: the word is whole and large at
         entry, its letters scatter across the header mid-section, then it
         splits W | ORKS and reassembles small. Scroll-driven, not timed. -->
    <div class="worksmark" aria-hidden="true">
      <div class="worksmark__row" id="worksRow">
        <span class="worksmark__l">W</span><span class="worksmark__l">O</span><span
          class="worksmark__l">R</span><span class="worksmark__l">K</span><span
          class="worksmark__l">S</span>
      </div>
    </div>

    <div class="ticker">
      <span class="ticker__slot" id="tickerSlot">WORKS</span>
      <span class="ticker__slot" style="text-align:right">MENU ::</span>
    </div>

    <div class="beat__inner">
      <div class="projects">
        {featured.map((p, i) => (
          <article class={`project ${sizes[i % sizes.length]}`} data-rise data-roles={p.roles.join('|')}>
            <a class="project__link" href={`/work/${p.slug}`}>
              <div class="project__media">
                <span class="project__ph">{p.video.type} &middot; {p.video.id}</span>
              </div>
              <h2 class="project__title">{p.title}</h2>
              <span class="project__go">Open project &#8627;</span>
            </a>
            <div class="project__roles">
              {p.roles.map((r) => <span class="meta">{r}</span>)}
            </div>
            <p class="project__note">
              {p.anecdote ?? 'Making-of anecdote goes here. Forty to sixty words, ending on the call JJ made rather than on the chaos.'}
            </p>
          </article>
        ))}
      </div>

      <p class="viewall" data-rise><a href="/work">View all &#8627;</a></p>
    </div>
  </section>

  <!-- ================= 06 LITTLE DOT ================= -->
  <section class="beat">
    <div class="beat__inner" data-rise>
      <p class="label">( the other half )</p>
      <h2 class="statement">Not everything I make is a film.</h2>
      <div class="prose" style="margin-top:2.5rem">
        <p>
          {LDS.channels} brand and creator channels. {LDS.views} views and {LDS.subs} subscribers
          across the tenure. Love Island packaging, Pokemon, originals programmed and written and
          thumbnailed.
        </p>
        <p class="meta" style="display:block;margin-top:1.5rem">
          FIRST PASS &mdash; needs JJ&rsquo;s framing. The argument to land: this is where the
          strategy half lives, and it is a different kind of proof than a beautiful film.
        </p>
      </div>
    </div>
  </section>

  <!-- ================= 07 HOW I WORK ================= -->
  <section class="beat">
    <div class="beat__inner" data-rise>
      <p class="label">( how I work )</p>
      <div class="principles">
        <p class="principle">
          All that range is why I&rsquo;d rather frame the whole project than own one piece of it.
          That&rsquo;s the job I want, and creative strategist is the closest name for it.
        </p>
        <p class="principle">
          &ldquo;Authentic&rdquo; is a vapid say-nothing word. Just like &ldquo;unique&rdquo;. Blah.
        </p>
        <p class="principle">
          I prefer, &ldquo;How do we position and frame this so it becomes what it&rsquo;s trying to
          be?&rdquo;
        </p>
      </div>

      <!-- Board 11's skills list was wired here; JJ ruled it out 2026-08-11.
           The prose carries the argument alone. -->
    </div>
  </section>

  <!-- ============ 07b SCROLL-TRANSFORMING ELEMENTS — board 12 ============ -->
  <section class="beat">
    <div class="beat__inner">
      <div class="morph" id="morph">
        <span class="wire-tag">Wire / board 12</span>
        <div class="morph__shape" id="morphShape">
          <svg viewBox="0 0 400 250" aria-hidden="true">
            <ellipse cx="200" cy="125" rx="150" ry="78" />
            <ellipse cx="230" cy="118" rx="96" ry="52" />
            <ellipse cx="185" cy="132" rx="118" ry="64" />
          </svg>
        </div>
        <div class="morph__cols">
          <div class="morph__col" data-morphcol>[ scribble text about me ]</div>
          <div class="morph__col" data-morphcol>[ scribble text about what I&rsquo;m capable of ]</div>
          <div class="morph__col" data-morphcol>[ other scribble text ]</div>
        </div>
      </div>
    </div>
  </section>

  <!-- ================= 08 NOW ================= -->
  <section class="beat beat--tight">
    <div class="beat__inner" data-rise>
      <p class="label">( now )</p>
      <div class="ph" style="max-width:40rem;padding:3rem 1.5rem">
        NOW
        <small>what JJ is building and learning right now</small>
        <small>Blender and AI animation pipeline &middot; MELM wrapping &middot; Embody</small>
      </div>
    </div>
  </section>

  <!-- ================= 09 COME IN ================= -->
  <footer class="beat" id="comein">
    <div class="beat__inner" data-rise>
      <p class="label">( come in )</p>
      <p class="statement" style="max-width:24ch">Tell me what you can already see.</p>
      <div class="cta">
        <a class="btn" href="mailto:jj@jjhouse.me"><span>Email me @</span></a>
        <!-- interim destination — JJ is picking a booking tool (likely a Google
             Workspace appointment schedule). Swap this href when he does. -->
        <a class="btn" href="mailto:jj@jjhouse.me?subject=Call"><span>Book a call &rarr;</span></a>
      </div>
      <div class="closing-mark"><Wordmark /></div>
    </div>
    <div class="colophon">
      <span class="meta">&copy; 2026 JJ House &middot; Los Angeles</span>
      <span class="meta">
        <a href="https://www.imdb.com/name/nm7420692/" style="color:inherit">IMDb</a> /
        <a href="https://www.instagram.com/jj.duplex/" style="color:inherit">Instagram</a>
      </span>
    </div>
  </footer>

  <script>
    // WebGL2 wordmark erosion. If it cannot start (no WebGL2, reduced
    // motion, shader failure) the DOM text simply stays visible and
    // nothing else on the page changes.
    import { initErode } from '../scripts/erode.js';
    const surface = document.getElementById('surface');
    if (surface) {
      let ok = false;
      try { ok = initErode(surface); } catch (err) { console.error('[erode]', err); }
      if (ok) surface.classList.add('is-canvas');
    }
  </script>

  <script is:inline>
    (function () {
      const reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

      // ---- the one-way flip. Light until the reel, dark forever after. ----
      const reel = document.getElementById('reel');
      if (reel) {
        const io = new IntersectionObserver(
          (entries) => {
            entries.forEach((e) => {
              if (e.isIntersecting) document.body.classList.add('is-dark');
            });
          },
          { rootMargin: '-45% 0px -45% 0px' }
        );
        io.observe(reel);
      }

      // ---- the reel recedes as you scroll through it ----
      const frame = document.getElementById('reelFrame');
      if (frame && reel && !reduce) {
        const onScroll = () => {
          const r = reel.getBoundingClientRect();
          const total = r.height - window.innerHeight;
          if (total <= 0) return;
          const p = Math.min(1, Math.max(0, -r.top / total));
          frame.style.setProperty('--recede', String(1 - p * 0.45));
        };
        addEventListener('scroll', onScroll, { passive: true });
        onScroll();
      }

      // ---- the credit ticker ----
      // Defaults to WORKS. When a project is in view it cycles that project's
      // own credits, half a second each, hard cuts like burned-in subtitles.
      // Rate of change is the information: multi-hat projects flicker,
      // single-hat projects sit still.
      const slot = document.getElementById('tickerSlot');
      const projects = [...document.querySelectorAll('[data-roles]')];
      if (slot && projects.length) {
        let active = null;
        let timer = null;
        let i = 0;

        const stop = () => { if (timer) { clearInterval(timer); timer = null; } };

        const play = (el) => {
          stop();
          if (!el) { slot.textContent = 'WORKS'; return; }
          const roles = (el.dataset.roles || '').split('|').filter(Boolean);
          if (!roles.length) { slot.textContent = 'WORKS'; return; }
          i = 0;
          slot.textContent = roles[0].toUpperCase();
          if (roles.length === 1 || reduce) return;   // one hat: sit still
          timer = setInterval(() => {
            i = (i + 1) % roles.length;
            slot.textContent = roles[i].toUpperCase();
          }, 500);
        };

        const io = new IntersectionObserver(
          (entries) => {
            entries.forEach((e) => {
              if (e.isIntersecting) { active = e.target; play(active); }
              else if (e.target === active) { active = null; play(null); }
            });
          },
          { rootMargin: '-35% 0px -35% 0px' }
        );
        projects.forEach((p) => io.observe(p));
      }

      // ---- board 5: the WORKS letters migrate ----------------------
      // Whole and large on entry, scattered across the header at the
      // midpoint, split W | ORKS and small on exit. Driven by scroll
      // position through the work section, so it reverses on scroll up.
      const row = document.getElementById('worksRow');
      const workSec = document.getElementById('work');
      if (row && workSec && !reduce) {
        const letters = [...row.querySelectorAll('.worksmark__l')];
        const n = letters.length;
        const lerp = (a, b, f) => a + (b - a) * f;
        const ease = (x) => (x < 0.5 ? 2 * x * x : 1 - Math.pow(-2 * x + 2, 2) / 2);

        const place = () => {
          const r = workSec.getBoundingClientRect();
          const span = r.height - window.innerHeight;
          if (span <= 0) return;
          const p = Math.min(1, Math.max(0, -r.top / span));
          const W = row.clientWidth || 1;

          letters.forEach((el, i) => {
            const bigPx = 34, smallPx = 15;
            // three keyframes: clustered left, scattered, split
            const startX = i * bigPx * 0.78;
            const midX = (i / (n - 1)) * (W - smallPx * 2);
            const endX = i === 0 ? 0 : W - (n - i) * smallPx * 0.95 - 4;

            let x, fs;
            if (p < 0.5) {
              const f = ease(p / 0.5);
              x = lerp(startX, midX, f);
              fs = lerp(bigPx, smallPx + 4, f);
            } else {
              const f = ease((p - 0.5) / 0.5);
              x = lerp(midX, endX, f);
              fs = lerp(smallPx + 4, smallPx, f);
            }
            el.style.transform = `translateX(${x}px)`;
            el.style.fontSize = fs + 'px';
            el.style.opacity = String(0.25 + 0.55 * (1 - Math.abs(0.5 - p) * 2) + 0.2);
          });
        };
        addEventListener('scroll', place, { passive: true });
        addEventListener('resize', place);
        place();
      }

      // ---- board 9: sound toggle, off by default -------------------
      const sound = document.getElementById('soundBtn');
      if (sound) {
        sound.addEventListener('click', () => {
          const on = sound.getAttribute('aria-pressed') === 'true';
          sound.setAttribute('aria-pressed', String(!on));
        });
      }

      // ---- board 12: text changes then disappears -------------------
      const cols = [...document.querySelectorAll('[data-morphcol]')];
      const morph = document.getElementById('morph');
      const shape = document.getElementById('morphShape');
      if (cols.length && morph && !reduce) {
        const onScroll = () => {
          const r = morph.getBoundingClientRect();
          const p = 1 - Math.min(1, Math.max(0, (r.top + r.height * 0.5) / window.innerHeight));
          if (shape) shape.style.transform =
            `rotate(${(p - 0.5) * 26}deg) scale(${0.85 + p * 0.3})`;
          cols.forEach((c, i) => {
            const local = Math.min(1, Math.max(0, (p - i * 0.14) * 2.4));
            c.style.opacity = String(local < 0.6 ? local / 0.6 : 1 - (local - 0.6) / 0.4);
            c.style.transform = `translateY(${(1 - local) * 22}px)`;
          });
        };
        addEventListener('scroll', onScroll, { passive: true });
        onScroll();
      }
    })();
  </script>
</Scroll>
